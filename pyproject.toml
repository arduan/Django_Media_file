[tool.poetry]
name = "LoadPic"
version = "0.1"
description = "Пример загузки файлов изображений"
authors = ["Vitalik"]
license = "12345"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
